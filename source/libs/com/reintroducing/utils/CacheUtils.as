package com.reintroducing.utils{	import flash.utils.getQualifiedClassName;		/**	 * Static utility methods used to manipulate cache settings on URLs.	 *  	 * @author Matt Przybylski [http://www.reintroducing.com] 	 * @version 1.0 	 */	public class CacheUtils 	{//- PRIVATE & PROTECTED VARIABLES -------------------------------------------------------------------------				//- PUBLIC & INTERNAL VARIABLES ---------------------------------------------------------------------------						//- CONSTRUCTOR	-------------------------------------------------------------------------------------------			public function CacheUtils()		{					}		//- PRIVATE & PROTECTED METHODS ---------------------------------------------------------------------------						//- PUBLIC & INTERNAL METHODS -----------------------------------------------------------------------------			/**		 * Cache-busts and returns a single URL.		 * 		 * @param $url A string value of the URL to bust.		 * 		 * @return String		 */		public static function bust($url:String):String		{			if ($url.indexOf("?") == -1)			{				$url += "?v=" + new Date().getTime();			}			else			{				$url += "&v=" + new Date().getTime();			}						return $url;		}				/**		 * Cache-busts and returns an array of URLs.		 * 		 * @param $arr An array that holds the string values of the URLs to bust.		 * 		 * @return Array		 */		public static function bustArray($arr:Array):Array		{			for (var i:String in $arr) $arr[i] += "?v=" + new Date().getTime();						return $arr;		}	//- EVENT HANDLERS ----------------------------------------------------------------------------------------				//- GETTERS & SETTERS -------------------------------------------------------------------------------------				//- HELPERS -----------------------------------------------------------------------------------------------			/**		 * @private		 */		public function toString():String		{			return getQualifiedClassName(this);		}	//- END CLASS ---------------------------------------------------------------------------------------------	}}