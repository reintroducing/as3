package com.reintroducing.utils{	import flash.media.Sound;
	import flash.utils.getQualifiedClassName;		/**	 * Static utility methods for working with sounds.	 *  	 * @author Matt Przybylski [http://www.reintroducing.com] 	 * @version 1.0 	 */	public class SoundUtils 	{//- PRIVATE & PROTECTED VARIABLES -------------------------------------------------------------------------				//- PUBLIC & INTERNAL VARIABLES ---------------------------------------------------------------------------						//- CONSTRUCTOR	-------------------------------------------------------------------------------------------			public function SoundUtils()		{					}		//- PRIVATE & PROTECTED METHODS ---------------------------------------------------------------------------						//- PUBLIC & INTERNAL METHODS -----------------------------------------------------------------------------			/**		 * Estimate the duration by calculating the bytes per second (sound.bytesLoaded / sound.length) and divide sound.bytesTotal by that number. 		 * That gives you an estimated duration. Just call this script repeatedly until the sound is finished loading, such as a 10ms timer.		 * NOTE: Works only on CBR mp3 files.		 * 		 * @param $sound The sound to calculate the duration of		 * 		 * @return Number The estimated length of the sound (in seconds)		 */		public static function calculateEstimatedDuration($sound:Sound):Number		{			if ($sound && $sound.length > 0)			{				return (($sound.bytesTotal / ($sound.bytesLoaded / $sound.length)) / 1000);			}						return -1;		}	//- EVENT HANDLERS ----------------------------------------------------------------------------------------				//- GETTERS & SETTERS -------------------------------------------------------------------------------------				//- HELPERS -----------------------------------------------------------------------------------------------			/**		 * @private		 */		public function toString():String		{			return getQualifiedClassName(this);		}	//- END CLASS ---------------------------------------------------------------------------------------------	}}